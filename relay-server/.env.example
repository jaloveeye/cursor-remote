# 스토어 선택: SUPABASE_URL 이 있으면 Supabase, 없으면 Redis 사용

# --- Supabase (선택) ---
# https://supabase.com 대시보드 > Project Settings > API
# relay 서버용으로는 service_role 키 권장 (RLS 없이 테이블 접근)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- Upstash Redis (SUPABASE_URL 없을 때 사용) ---
# https://console.upstash.com 에서 Redis 생성
UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token-here
