{
  "name": "cursor-remote-relay",
  "version": "1.0.0",
  "description": "Cursor Remote Relay Server for Vercel",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/node": "^4.0.0",
    "typescript": "^5.3.0",
    "vercel": "^41.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
